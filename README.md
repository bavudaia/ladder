# PrepHero · The Ladder

A ranked interview-prep season for senior SWE interviews. You do the practice *inside* the app — problems, mocks, and lessons are generated by Claude, your answers are graded by Claude, and points are logged automatically from what you actually finished. There is no manual entry form.

Open `index.html` in a browser. No build step.

## The ten activities

Everything on the training floor is a real session with generated content and written feedback.

| Activity | Points | What happens |
| --- | --- | --- |
| System design mock | 100 | Multi-turn round with an AI interviewer that probes scoping, capacity, API, data model, hot keys, and failure modes |
| Coding mock | 100 | Live coding round — approach, complexity, edge cases, a trace of your own code, and how you'd test it |
| Deep-dive + explain-back | 60 | Read a generated study brief, then explain it back from memory and get graded on precision |
| DSA hard / medium / easy | 50 / 30 / 15 | A generated problem with examples, constraints, and escalating hints. Submit approach + code |
| STAR story polish | 40 | Draft Situation / Task / Action / Result and get it critiqued and rewritten |
| Rapid-fire behavioral | 25 | Five behavioral questions back to back, graded together |
| Concept review | 20 | Five recall questions on one fundamental, with reference answers revealed at grading |
| Hello Interview lesson | 10 | A short dense lesson plus two check questions |

**Consistency bonus:** your first session each day earns `5 × streak`, capped at +30/day.

Points are awarded for *completing* a session, at the values above. The grade (0–100 and a bar rating from *Below bar* to *Strong senior*) is separate — it tells you how it went, it doesn't change the payout. Showing up is the game; the grade is the feedback.

## The season

**The current season opens on 1 August 2026** — that's Day 1. Until then the app shows a `T-` countdown and the chart holds.

Two constants at the top of the script control this: `SEASON_EPOCH` (the opening date) and `SEASON_SERIAL`. Changing either wipes progress and arms a fresh season the next time you open the page, keeping your API key and preferences — bump the serial to restart on the *same* date you already have. "Reset season" in Settings starts a season from today instead (or from the epoch date, if that's still in the future).

Nine rivals matched to your skill level — three grinders, three steady, three casual. They train once per real calendar day whether or not you open the app, so falling behind is real. Standings, division (Unranked → Master), and the trajectory chart all update from that.

## Voice

Every session can be run out loud. **Voice mode** (toggle in the session header) reads each interviewer question aloud, then opens the mic automatically — you answer by speaking, and after a few seconds of silence the turn sends itself and the next question comes back. That's a hands-free mock interview, which is the only way this kind of practice matches the real thing.

Outside voice mode there's a **Speak** button on every answer field for dictation, a ▶ on every interviewer turn and question to replay it, and a *Read feedback aloud* button on the report. The code field has no mic — dictating code is useless.

### Voice commands

Say **"hero"** followed by a command — either while the mic is already open in a session, or via the **Command** button on the training floor. The wake word is stripped, so it never lands in your answer.

`hero, start a coding mock` · `hero, next question` · `hero, repeat that` · `hero, send it` · `hero, submit` · `hero, hint` · `hero, what's my rank` · `hero, be quiet` · `hero, exit`. The `?` button lists them all.

Matching is a local grammar first — instant, free, works with no key. If a phrasing isn't in the grammar and a key is set, it falls back to one short Claude call that maps free speech onto an intent, so "kick off a design round" still works.

Speech runs entirely in your browser via the Web Speech API. **The Claude API has no audio surface** — it accepts text, images, and PDFs and returns text — so only the transcript is ever sent, and voice costs nothing per session. Settings covers the speaking voice, rate, recognition language, and the auto-send silence delay (set it to 0 to always send manually).

Recognition needs Chrome, Edge, or Safari — Firefox doesn't implement it, and there playback still works while dictation is hidden. It also needs a secure origin, so if the mic is blocked, serve the folder (below) instead of opening the file directly. Playback and dictation never run at once, so the mic can't hear the interviewer's voice.

## Diagrams

System design mocks, coding mocks, deep-dives, and all three DSA activities take image attachments — a whiteboard photo, a screenshot from Excalidraw or Whimsical, a page of handwritten code. **Click Attach, drag a file onto the panel, or just paste a screenshot** (paste is the one you'll use; it's how drawing tools export).

In a mock, the diagram travels with the answer you send, and the interviewer's next question is about what's actually in it — the component with no owner, the arrow with no protocol, the store with no partition key, the box that can't meet the number you quoted. On a submission it's graded alongside your text.

Images are downscaled and re-encoded in the browser before they go anywhere: 1400px long edge, PNG for line art where it fits, JPEG when it doesn't, 500 KB per image and 1.5 MB per session. That's for the browser's ~5 MB `localStorage` budget, which the whole season shares. If it fills anyway, the season still saves — the image bytes get dropped rather than your progress, and you're told which diagrams didn't survive the reload.

**Offline mode can't read images.** Keyword grading has nothing to say about a picture, so it tells you the diagram wasn't graded instead of quietly scoring around it. STAR, rapid-fire, concept review, and lessons take no attachments — a diagram adds nothing there.

## Profiles

The app opens on a lock screen. Create a profile — a name and a password — and everything past it is yours: your own season, your own log, your own key.

**What the password actually does.** Be clear-eyed about this. A password check written in client-side JavaScript stops nobody who opens the dev tools, and there is no server here to check one properly. What the password *does* do is real, though: your Anthropic API key is encrypted with AES-GCM under a key derived from it (PBKDF2-SHA256, 310k iterations), so the plaintext key exists only in the tab, only while you're unlocked. Lock it, or close the tab, and what's left on disk is ciphertext. Someone who opens your browser can read your season; they cannot spend your API credits.

Consequences worth knowing:

- **You re-enter the password each time you load the page.** That's the cost of never writing the key to disk in the clear. It's a password manager, not a cookie.
- **There is no password reset.** Forgetting it costs you the stored key, not your season — make a new profile and paste a key again.
- **Profiles are per browser.** They live in `localStorage`; they don't follow you to another device.
- **Encryption needs a secure origin.** On `https://` or `http://localhost` you're fine. If you open the file directly and the browser refuses, the lock screen tells you to serve the folder instead.

Upgrading from a version without profiles is automatic: the first profile you create adopts your existing season and your plaintext key, then deletes the plaintext copy.

## AI setup

Paste an Anthropic API key into **Settings**. It's encrypted under your profile password (see above) and sent directly to `api.anthropic.com` — nothing else sees it. Default model is Claude Opus 5; Sonnet 5 and Haiku 4.5 are selectable if you want cheaper or faster sessions.

If API calls fail with a network error when opening the file directly, serve the folder instead:

```
python3 -m http.server 8000
```

then open `http://localhost:8000`.

**Without a key the app still works.** It falls back to a built-in bank of problems, mocks, briefs, and question sets, and grades your answers against per-item rubrics — it can tell you what you left out, but not whether what you wrote is correct. Every session and every log entry is tagged `AI` or `OFFLINE` so you always know which produced the feedback. The same fallback catches a failed or refused API call mid-session.

## Hosting it

**`index.html` contains no secret.** The only `sk-ant-` string in the file is the placeholder text in the Settings input. Your key lives in *your* browser, put there by you. Publishing this file exposes nothing — a stranger who finds the URL gets an empty app, no key, no season, and no way to spend your credits. Host it without fear; the reason to add a lock is that you'd rather strangers didn't use your prep app, not that they could steal anything.

Hosting also *fixes* something: speech recognition and WebCrypto both need a secure origin, so voice and profiles work properly over `https://` in a way they may not from `file://`.

### Two modes, same file

The app asks the deployment who you are on load, and behaves accordingly.

**Local** — opened as a file, or served from a dumb static host. Profiles, a password-encrypted key, a season in this browser only. Nothing below applies.

**Hosted** — deployed to Cloudflare Pages behind Access, with the `functions/` in this repo. Then:

- **One password guards it.** You set `APP_PASSWORD` as a deployment secret; the app trades it for an HttpOnly session cookie signed with a key derived from that password. Cloudflare Access is supported too and preferred when configured (`ACCESS_TEAM_DOMAIN` + `ACCESS_AUD`) — it is the stronger gate, but it needs Zero Trust, which wants a card on file even on the free plan.
- **The key never reaches a browser.** It's a deployment secret; sessions call your own `/api/claude`, which adds it server-side. A stolen laptop leaks nothing.
- **One season, every device.** Progress round-trips through Cloudflare KV keyed by your email, so your phone and your laptop climb the same ladder.

**[DEPLOY.md](DEPLOY.md) has the fifteen-minute walkthrough** — deploy, KV namespace, the key as a secret, the Access policy, and the two variables the Functions need to verify it. Everything is inside Cloudflare's free tier.

Two devices that both practised offline **merge** when they reconnect: the app takes the union of both session logs and recomputes points and streak from it, because the log is the ground truth and every entry is timestamped. Last-write-wins would silently delete whichever session you did second. Diagrams are the exception — they're stripped before syncing, since they're megabytes of base64 belonging to the device that took the screenshot.

### Alternatives

| | |
| --- | --- |
| **Netlify / Vercel** | Drag-and-drop deploy, just as easy, but you get local mode only — no key proxy, no cross-device sync. Their password protection is a paid feature. |
| **GitHub Pages** | Free and fine — the file has no secret — but no access control at all, and no server side, so local mode only. |
| **Tailscale Serve** | If "anywhere" means your own devices, this publishes it on your private tailnet with no public exposure. Local mode. |

## Data

Progress lives in `localStorage` under `prephero_ladder_v2::<profile>`, including any session you leave half-finished — close the tab mid-mock and it's waiting when you come back. Profiles themselves (salt, password verifier, encrypted key) are under `prephero_accounts`. "Reset season" in Settings wipes the current profile's progress (click twice to confirm) but keeps your API key.

`index-v1-backup.html` is the previous version of the tracker, kept because this folder isn't under version control.

## Tests

```
python3 tests/run_all.py
```

No dependencies and no build step: the suites pull the `<script>` out of `index.html`, append an export hook, and run it under JavaScriptCore (which ships with macOS) against a stubbed DOM. `fetch`, `localStorage`, the Web Speech API, `FileReader`, `Image`, and `canvas` are all faked, which is what lets the Claude wire format and the image pipeline be asserted without an API key or a single token spent.

| Suite | Covers |
| --- | --- |
| `test_auth.py` | The lock screen, password rules, that the API key is never on disk in plaintext, wrong passwords failing closed, per-profile season isolation, the v1 upgrade path, and the insecure-origin message |
| `test_sessions.py` | All ten session flows end to end, scoring and the consistency bonus, the ledger, voice, browsers without speech, `file://` mic blocking, and the season reset/resume lifecycle |
| `test_api.py` | Request headers, streaming, structured-output schema legality, per-model capabilities, and every activity's generation and grading prompt |
| `test_commands.py` | The voice-command grammar, wake-word extraction, and commands executed against live sessions |
| `test_images.py` | File validation, the downscale/re-encode loop, the content-block wire format, per-turn and per-session budgets, and quota-exhausted saves |

Each file also runs standalone (`python3 tests/test_images.py`). Generated JS lands in `tests/.build/`.
